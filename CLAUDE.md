## User note
User has flagged repeated confusion about how this tool works relative to their GitHub/filesystem view: they can see the GitHub repo, their local filesystem, and their browser, but have no visibility into what state this tool sees/edits. Be extra explicit about what was changed and why, avoid assuming familiarity with the editing workflow.
